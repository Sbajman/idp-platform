apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources: []

labels:
  - pairs:
      platform.example.com/managed-by: argocd
      platform.example.com/part-of: idp-platform

    includeSelectors: false
    includeTemplates: true